#include <iostream>
#include "BST.h"
#include "firsttier.cpp"
#include <string>
using namespace std;
int main(){
	first a;
	a.build();
	//p = tree.createNode(4,"4", tree.createNode(3, tree.createNode(1,nullptr, nullptr), tree.createNode(2,nullptr, nullptr)), tree.createNode(5, nullptr, nullptr));
	return 0;
}